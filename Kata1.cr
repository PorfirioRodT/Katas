palindromes = ["ana", "oso", "true", "anita lava la tina", "madre"]

def palindrome(p)
  standar = String(p).lower().replace(" ", "")
  return standar == standar[-1]
end 

name = ""

if name == "ok"
  loop do
    resultado = palindrome(p)
    puts(resultados)
  break if palindrome != true
  end
end
